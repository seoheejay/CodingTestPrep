#난이도 1, 시간 제한 1초, 메모리 제한 128MB, 기출 D 기업 프로그래밍 콘테스트 예선

n = int(input())

array = []
for i in range(n):
  input_data = input().split()
  #이름은 문자열 그대로, 점수는 정수형으로 변환하여 저장
  array.append = ((input_data[0], int(input_data[1])))

#키 이용하여, 점수를 기준으로 정렬
array = sorted(array, key = lambda studendt: student[1])

for student in array:
  print(student[0],end=' ')
